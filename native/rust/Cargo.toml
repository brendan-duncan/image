[package]
name = "image_native"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
image = { version = "0.25.6", default-features = true, features = ["jpeg", "png"] }
